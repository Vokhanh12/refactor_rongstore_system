version: "3.9"

services:
  redis:
    image: redis:7
    container_name: redis_cache
    restart: unless-stopped

    ports:
      - "${REDIS_PORTS}:6379"

    command: redis-server --appendonly yes

    volumes:
      - redis_data:/data

volumes:
  redis_data: